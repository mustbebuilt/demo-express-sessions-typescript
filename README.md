# Demo Express Sessions

This is a basic Express app for demoing basic session.

```terminal
npm install express-session
```

Assuming Node and Nodemon installed to set up run:

```terminal
npm install
nodemon app
```
